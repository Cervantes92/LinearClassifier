import numpy as np
import csv

fileName = "xyTrainingData.csv"
points = []

with open(fileName, 'rb') as csvfile:
	filereader = csv.reader(csvfile, delimiter = ",", quotechar = "|")

	i = 0
	for row in filereader:
		for j in range(0,2):
			points[i].append(float(row[j])		i += 1

print points[33][0] ** 2
